prepare all documentation for 1.0.0 release.  
generate an animated gif giving a tour of the web interface. 
- animation should start with a refresh so we get the logo splash screen
- then start a new session of type opencode with name "datawatch-opencode", the LLM backend takes a few seconds to load, when it's ready - in the new session window, open the llm backend dialog box for 3 seconds so people can see the list, then select opencode and start so we can see it start with gui. 
- wait until waiting_input 
- send command: provide a summary of https://github.com/dmz006/datawatch
- then go back to session list then start a new session named "datawatch-claude" selecting claude-code.  
- There are 2 acceptance prompts that need to be entered, but it's just sending enter at waiting_prompt for the first 2 prompts.
- there will be a few popup alerts, wait until they are all done
- send command: provide a summary of https://github.com/dmz006/datawatch
- when it's back at waiting input, go back and start a new session 
- start a new session, with name 'datawatch-shell' and select shell as llm and start it
- wait 3 seconds
- send command: datawatch -h
- send command: datawatch status
- send command: datawatch stats
- wait 1 second
- send command: datawatch session list
- wait 1 second
- send command: datawatch completion -h
- wait 1 second
- send command: datawatch setup
- wait 1 second
- send command: session=`datawatch session list|grep datawatch-shell|cut -d" " -f 1`
- send command: datawatch session send $session "ps"
- send command: datawatch session
- wait 3 seconds
- send command: session=`datawatch session list|grep datawatch-claude|cut -d" " -f 1`
- send command: datawatch session tail -n 45 24a3
- send command: datawatch about
- wait 3 seconds
- go to to the alerts tab and look at the active and then inactive tabs; 3-4 seconds each 
- then go to the settings tab and scroll to the bottom taking 3-4 seconds 
- then go through each settings sub-menu tab again slowly scrolling to the bottom then after about stop the video. 
save it on the main readme page under the datawatch icon. 
