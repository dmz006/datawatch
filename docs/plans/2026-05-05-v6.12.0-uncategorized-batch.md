# v6.12.0 — uncategorized batch + Option A badge

Operator-directed 2026-05-05. Closes the entire `## Unclassified` section of `docs/plans/README.md` plus Option A federated-peer-stale badge breadcrumb. Ships as a single minor release.

## Scope

| # | Item | Size | Approach |
|---|------|------|----------|
| 1 | Option A badge breadcrumb | S | cog badge → click navigates to Observer → Federated Peers; per-peer-card stale dot; new locale strings |
| 2 | `datawatch-definitions.md` central docs | L | scaffold the doc with all tab/card sections + brief blurbs; wire `?` icon → opens anchor; **full prose population is iterative** — scaffold + 4 example sections + rule for future cards |
| 3 | Sessions tab `?` icon (left of search) | S | matches Automata pattern; links into central doc |
| 4 | In-session `?` icon (left of conn indicator) | S | same as above |
| 5 | Sessions list: ungray clickable items on done sessions | S | CSS — `.session-card.done .clickable-action { opacity: 1; }` |
| 6 | Automata tab help → central docs | S | swap href in help overlay |
| 7 | Multi-select all/delete/cancel above bottom buttons | S | match Sessions pattern (already correct there); fix Automata |
| 8 | New automaton form layout fix | M | redesign form as compact 2-column wizard; verify no overlapping; readable fonts; "Start from template" first |
| 9 | Card buffer/spacing fixes (long list) | M | same `.settings-card` padding/spacing pattern that fixed the v6.11.x cards earlier |
| 10 | Settings/About: drop "Branding/Splash" card | S | remove from About tab render |
| 11 | Settings/About: "System documentation & diagrams" → docs landing | S | swap link target; landing has intro + howto/arch/diagram links + anchor index |
| 12 | Observer audit log default = 5 entries | S | UI default; real `?count=` query stays |
| 13 | Root README cleanup + restore Daniel Keys Moran ack | S | strip PAI table + BL refs from current-release summary; restore exact DKM ack; add "Additional Acknowledgements" header |

## Implementation order (ship in two commits inside v6.12.0)

### Commit 1 — UX/CSS layer (low-risk, no daemon changes)

5, 6, 9, 10, 12 — all PWA-only. Verifiable by reload.

### Commit 2 — daemon + structural

1 (badge breadcrumb), 2 (docs scaffold), 3, 4 (? icons), 7 (multi-select position), 8 (form layout), 11 (about → landing), 13 (README).

### Out of scope (deferred — call out in release notes)

- **Item 2 prose population**: scaffold + 4 example card descriptions ship in v6.12.0; the rest of the cards keep placeholder "TODO" entries with a tracker in `docs/plans/README.md` listing every card still needing prose. This keeps the rule (every new card adds a docs section) live without blocking the release on an exhaustive write-up of every existing card.
- **Item 7 / 8 / 9 mobile parity**: file `datawatch-app#` issues for the three substantive changes the Compose Multiplatform side should mirror.
- **Automata smoke check**: operator-noted as the remaining smoke gap. Plan separately as v6.12.x follow-up.

## Daniel Keys Moran ack — exact text to restore

(Per the operator's explicit instruction "Put that back, exactly how it was.") Will recover from `git log --diff-filter=D -p -- README.md` and reinstate verbatim.

## datawatch-app issues to file

- **#74** (item 5): "Done-state sessions: keep clickable affordances ungrayed (last response, restart, delete, reorder) — match new PWA behaviour"
- **#75** (item 7+8): "Automata: multi-select bar position + new-automaton form layout pass"
- **#76** (item 1): "Federated peer stale indicator on per-peer card + tap-to-navigate from cog badge"
- **#77** (item 2): "Central documentation index + per-card `?` icon helper deep links"

## Acceptance

1. All 13 items above visibly addressed (PWA reload).
2. Smoke 104+ pass / 0 fail (no claude-code variant, default opencode).
3. `1804+ go test ./...` pass.
4. README contains DKM ack verbatim.
5. `docs/plans/README.md` `## Unclassified` section reduced to the single deferred prose-population tracker entry.
6. Four datawatch-app issues filed.

Ship target: **v6.12.0** (minor — adds central docs system + UX overhaul batch).
