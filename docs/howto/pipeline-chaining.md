# How-to: Pipeline + session chaining

> **Status: stub.** Full walkthrough pending. In the meantime:
> the pipeline reference lives in [`docs/api/`](../api/) (search
> for `pipeline_*` MCP tools) and the design overview is in
> [`docs/architecture-overview.md`](../architecture-overview.md).

This how-to will cover chaining tasks into DAG pipelines with
before/after gates, combining pipeline tasks with manually-managed
sessions, examples of pre-/post-test gates that catch regressions
between steps. Track at [BL190](../plans/README.md).
