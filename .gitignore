.mcp.json
bin/
*.log
*.db
config.yaml
.env
__debug_bin
/datawatch
*.test
coverage.out
.claude/
*.swp
channel/node_modules/
channel/dist/
internal/stats/bpf/vmlinux.h
.venv/

# F10: container build env (registry creds + IPs)
.env.build
dist/

# Local-environment leak prevention (AGENT.md "No local-environment
# leaks in git" rule). Operator-specific values stay out of the repo.
*.kubeconfig
kubeconfig
.kube/
secrets.yaml
*.sealed-secret.yaml
*.pem
*.key
*.crt
!docs/**/*.pem.example
!docs/**/*.key.example
/internal/server/web/docs/
# BL274 (v6.16.0) — regenerated at every build by `make docs-index`.
# Same pattern as the embedded docs mirror above: source-of-truth is
# the docs/ tree; the index is a build artifact.
/internal/docsindex/assets/docs-bm25-index.json
internal/memory/memory-wal.jsonl

# datawatch-managed (BL219)
.aider.conf.yml
.aider.chat.history.md
.aider.tags.cache.v*/

# datawatch-managed (BL219)
.goose/

# datawatch-managed (BL219)
.gemini/
internal/council/council/

# E2E test isolation — ephemeral daemon data dir
.datawatch-test/

# E2E test run evidence (generated output, not source).
# The runs/ folder itself is tracked via docs/testing/runs/.gitkeep;
# individual run directories and their evidence are gitignored.
docs/testing/runs/2*/
