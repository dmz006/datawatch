apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "datawatch.fullname" . }}-config
  labels:
    {{- include "datawatch.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | toYaml | indent 4 }}
